<%= form_for(@client) do |f| %>
  <%= f.label(:partner) %>
  <%= f.collection_select(:partner_id, @partners, :id, :name) %>
  <%= f.text_field :name, :placeholder => 'Enter Client Name' %>
  <%= f.submit %>
<% end %>